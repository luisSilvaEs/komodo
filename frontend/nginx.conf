server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Fallback to index.html for all routes so React Router works on hard refresh
    location / {
        try_files $uri $uri/ /index.html;
    }
}